{%extends 'layout/admin/base.twig'%}
{%block content%}
    <legend>Ajout de compétence professionnelles</legend>
    <form id="formEn" novalidate action="{{ path('CV_skills_add') }}" method="post">
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Ajouter">
    </form>
{%endblock%}